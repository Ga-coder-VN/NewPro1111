specVersion: "v1"
serviceId: "newpro"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "newpro-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
